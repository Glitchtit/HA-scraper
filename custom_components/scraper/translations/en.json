{
  "config": {
    "step": {
      "user": {
        "title": "Connect to HA-Storage & K-Ruoka",
        "description": "Enter your HA-Storage instance URL and the K-Ruoka store ID. These settings are required to search for products and add them to Storage.",
        "data": {
          "storage_url": "Storage URL",
          "store_id": "K-Ruoka Store ID"
        },
        "data_description": {
          "storage_url": "Base URL of your HA-Storage instance, e.g. http://a0a9ed235_ha_storage:8099",
          "store_id": "K-group store ID, e.g. N110 for K-Supermarket Helsinki"
        }
      }
    },
    "error": {
      "required": "This field is required",
      "already_configured": "This Storage + store combination is already configured"
    },
    "abort": {
      "already_configured": "This Storage + store combination is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Scraper Settings",
        "description": "Configure optional features such as product image uploading.",
        "data": {
          "upload_images": "Upload product images to Storage",
          "use_graphql": "Use GraphQL backend (recommended)"
        },
        "data_description": {
          "upload_images": "Download and upload product images when creating new products",
          "use_graphql": "Use the faster GraphQL backend; disable only if you need the kr-api fallback"
        }
      }
    }
  }
}
